{% extends 'cpanel/content.html.twig' %}

{% block title %}{{ parent() }} - Редактировать покупателя{% endblock %}

{% block content %}
    <div class="content-wrapper">
        {% block breadcrumbs %}
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('cpanel.index') }}">Главная</a></li>
                <li class="breadcrumb-item"><a href="{{ path('cpanel.buyers') }}">Покупатели</a></li>
                <li class="breadcrumb-item active">Профиль</li>
            </ol>
        {% endblock %}

{#        <div class="row">#}
{#            <div class="col-lg-12 grid-margin stretch-card mt-2">#}
{#                <a href="javascript:void(0)"#}
{#                   class="btn btn-info btn-fw mr-4" data-toggle="modal" data-target="#assign-salon"#}
{#                   data-id="{{ dealer.id }}">Заказы клиента</a>#}
{#        </div>#}
        {% include 'cpanel/_flashes.html.twig' %}

        <div class="row">

            <div class="col-lg-12 grid-margin stretch-card">

                <div class="card">
                    <div class="card">
                        {{ include('cpanel/buyers/_form.html.twig',{
                            name_sumbit:'Редактировать профиль'
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}