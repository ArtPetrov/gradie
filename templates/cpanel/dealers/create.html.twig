{% extends 'cpanel/content.html.twig' %}

{% block title %}{{ parent() }} - Добавить дилера{% endblock %}

{% block content %}
    <div class="content-wrapper">
        {% block breadcrumbs %}
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('cpanel.index') }}">Главная</a></li>
                <li class="breadcrumb-item"><a href="{{ path('cpanel.dealers') }}">Дилеры</a></li>
                <li class="breadcrumb-item active">Добавить дилера</li>
            </ol>
        {% endblock %}
        {% include 'cpanel/_flashes.html.twig' %}

        <div class="row">

            <div class="col-lg-12 grid-margin stretch-card">

                <div class="card">
                    <div class="card-body">
                        <h2 class="card-title">Добавить дилера</h2>
                        <p class="card-description"></p>
                        {{ include('cpanel/dealers/_form_create.html.twig',{
                            name_sumbit:'Создать дилера'
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}