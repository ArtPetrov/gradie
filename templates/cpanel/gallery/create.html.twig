{% extends 'cpanel/content.html.twig' %}

{% block title %}{{ parent() }} - Создать альбом{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('cpanel.alert.error') }}
    {{ encore_entry_script_tags('cpanel.gallery') }}

{% endblock %}

{% if form is defined %}
    {% form_theme form with [
        'cpanel/_form_errors.html.twig',
        'cpanel/gallery/_form_images_entry.html.twig',
        'cpanel/gallery/_form_radio.html.twig'
    ] %}
{% endif %}

{% block content %}
    <div class="content-wrapper">
        {% block breadcrumbs %}
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('cpanel.index') }}">Главная</a></li>
                <li class="breadcrumb-item"><a href="{{ path('cpanel.gallery') }}">Галерея</a></li>
                <li class="breadcrumb-item active">Создать страницу</li>
            </ol>
        {% endblock %}
        {% include 'cpanel/_flashes.html.twig' %}

        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    {{ include('cpanel/gallery/_form.html.twig',{
                        name_sumbit:'Создать альбом'
                    }) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}