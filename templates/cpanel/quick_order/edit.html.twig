{% extends 'cpanel/content.html.twig' %}

{% block title %}{{ parent() }} - Редактировать заявку на быстрый заказ{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('cpanel.alert.error') }}
{% endblock %}

{% if form is defined %}
    {% form_theme form with [
        'cpanel/_form_errors.html.twig'
    ] %}
{% endif %}

{% block content %}
    <div class="content-wrapper">
        {% block breadcrumbs %}
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('cpanel.index') }}">Главная</a></li>
                <li class="breadcrumb-item"><a href="{{ path('cpanel.quick.orders') }}">Быстрые заказы</a></li>
                <li class="breadcrumb-item active">Редактировать заказ</li>
            </ol>
        {% endblock %}

        {% include 'cpanel/_flashes.html.twig' %}

        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    {{ include('cpanel/quick_order/_form.html.twig',{
                        name_sumbit:'Редактировать заказ'
                    }) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}