{% extends 'cpanel/content.html.twig' %}

{% block title %}{{ parent() }} - Создать опрос{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('cpanel.tinymce') }}
{% endblock %}

{% if form is defined %}
    {% form_theme form with [
        'cpanel/_form_errors.html.twig',
        'cpanel/quiz/_form_checkbox.html.twig'
    ] %}
{% endif %}

{% block content %}
    <div class="content-wrapper">
        {% block breadcrumbs %}
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('cpanel.index') }}">Главная</a></li>
                <li class="breadcrumb-item"><a href="{{ path('cpanel.quizs') }}">Опросы</a></li>
                <li class="breadcrumb-item active">Создать опрос</li>
            </ol>
        {% endblock %}
        {% include 'cpanel/_flashes.html.twig' %}

        <div class="row">

            <div class="col-lg-12 grid-margin stretch-card">

                <div class="card">
                    <div class="card-body">
                        <h2 class="card-title">Создать опрос</h2>
                        <p class="card-description"></p>
                        {{ include('cpanel/quiz/_form.html.twig',{
                            name_sumbit:'Опубликовать опрос'
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}